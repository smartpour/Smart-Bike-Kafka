# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_CLIENT_ID=smart-bike-client

# Migration Control Flags
USE_KAFKA=true
USE_MQTT=false

# Device Configuration
DEVICE_ID=000001

# Sensor Device Prefixes
HEART_RATE_DEVICE_PREFIX=TICKR
CADENCE_DEVICE_PREFIX=RPM
SPEED_DEVICE_PREFIX=SPD

# Legacy MQTT Configuration (for sensors still using MQTT)
MQTT_HOSTNAME=
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_PORT=1883

# Database
MONGODB_URL=mongodb://localhost:27017/smart-bike
